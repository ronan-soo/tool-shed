'use client';

import { CardDescription } from '@/components/ui/card';

export function StringifyBlock() {
  return (
    <CardDescription>
      This block converts a JSON object back into a string. No options needed.
    </CardDescription>
  );
}
