'use client';

import { CardDescription } from '@/components/ui/card';

export function ParseJsonBlock() {
  return (
    <CardDescription>
      This block parses the input string into a JSON object. No options needed.
    </CardDescription>
  );
}
